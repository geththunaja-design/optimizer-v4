# Optimizer V.4.0 - bundling the web layer into the APK
#
# 1) Copy this file to the repository root as ".gitignore" (the perchance editor refuses
#    to store a file whose name starts with a dot, which is why it lives here).
# 2) Then it keeps build output and the generated assets out of git.
#
# ---------------------------------------------------------------- contents ---

.gradle/
build/
app/build/
local.properties
.cxx/
*.apk
*.aab
*.keystore
*.jks

# generated by tools/sync-web-assets.sh from ../../index.html and ../../src
app/src/main/assets/www/
